@model IEnumerable<Scrum3.Model.Luokkatilat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Luokka)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Luokka)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LuokkaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LuokkaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LuokkaID })
        </td>
    </tr>
}

</table>
